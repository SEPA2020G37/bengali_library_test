<div class="nav-row">
  <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark" id="navbar">
    <a href="/user_dashboard" class="navbar-brand">
      <img
        src="resources/New_logo.png"
        width="100"
        height="100"
        class="d-inline-block align-middle"
        alt="BCLibrary"
      />
    </a>
    <button
      class="navbar-toggler"
      data-toggle="collapse"
      data-target="#navbarMenu"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarMenu">
      <% if(user) {%>
        <% if(user.positionId == 1){ %>
          <% if(title == "book Store" ) {%>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item">
                <a href="/user_dashboard" class="nav-link">User Dashboard</a>
              </li>
            </ul>
          <% } else { %>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item">
                <a href="/myWishList" class="nav-link">Wishlist</a>
              </li>
              <li class="nav-item">
                <a href="/book-store" class="nav-link">Book Store</a>
              </li>
            </ul>
          <% } %>
        <% }if(user.positionId == 2){ %>
            <ul class="navbar-nav mr-auto">
              <li class="nav-item">
                  <a href="#" class="nav-link">Generate Statistics Reports</a> 
              </li>
              
              <!-- <li class="nav-item">
                  <a href="#" class="nav-link name">Hi! Username </a>
              </li> -->
            </ul> 
        <% }%>
        <ul class="navbar-nav nav justify-content-end">
          <% if(title == "book Store" ) {%>
            <li class="nav-item">
              <a id="viewCart" class="nav-link" href="#"><i class="fas fa-shopping-cart fa-2x "></i></a>
            </li>
          <% } %>
          <li class="nav-item dropdown">
            <button class="btn bg-transparent nav-link" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-user-circle fa-4x"></i>
            </button>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
              <p><%= user.firstName %></p>
              <a class="dropdown-item" href="#">Logout</a>
            </div>
          </li>
        </ul>
      <% } else {%>
        <ul class="navbar-nav mr-auto">
          <!-- you can put some thing here if you want
            it will display on the left side of navigation bar.-->
          <li class="nav-item">
            <a href="/book-store" class="nav-link">Book Store</a>
          </li>
        </ul>
        <ul class="navbar-nav nav justify-content-end">
          <li class="nav-item">
            <a class="nav-link" href="/register">Register</a>
          </li>
          <li class="nav-item">
              <a class="nav-link" href="/login">Login</a>
          </li>
        </ul>
      <% } %>
     </div>
  </nav>
</div>
