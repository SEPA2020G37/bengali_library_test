<div class="nav-row">
    <nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark" id="navbar">
        <a href="#" class="navbar-brand">
            <img src="resources/logo.png" width="100" height="100" class="d-inline-block align-middle" alt="BCLibrary" />
        </a>
        <button class="navbar-toggler" data-toggle="collapse" data-target="#navbarMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarMenu">
            <ul class="navbar-nav mr-auto">
                <% if(user){ %>
                    <li class="nav-item">
                        <a href="/user_dashboard" class="nav-link">User Dashboard</a>
                    </li>
                <% }%>
            </ul>
            <ul class="navbar-nav nav justify-content-end">
                <% if(user){ %>
                    <li class="nav-item dropdown">
                        <button class="btn bg-transparent nav-link" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                          <i class="fas fa-user-circle fa-4x"></i>
                        </button>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                          <a class="dropdown-item" href="#"><%= user.firstName %></a>
                          <a class="dropdown-item" href="#">Logout</a>
                        </div>
                    </li>
                    <li class="nav-item">
                        <a id="viewCart" class="nav-link" href="#"><img src="resources/cart.png" style="width: 60px; height: 60px;" ></a>
                    </li>
                <% }else{ %>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </nav>
</div>